# ドメイン用語

## 購入候補

公式トレードサイトの即時購入操作を起点に保存される、購入結果が未確定のレコード。状態は`pending`。

## 購入済み

ユーザーが購入結果を手動で確定したレコード。状態は`purchased`。

## 操作取り消し（Undo）

直前の単件操作を短時間だけ元の状態へ戻す回復操作。通常の状態遷移とは別の安全弁であり、購入済みを恒久的に未確定へ戻す編集機能ではない。

## Undo期限

操作取り消しが利用できる30秒間の時間枠。画面には残り秒数と円形プログレスで表示する。期限を過ぎた操作は取り消せない。

## 最新Undo

同時に保持する操作取り消しは最新の単件操作1件だけとする。別の変更操作が完了した時点で、以前のUndoは無効になる。
